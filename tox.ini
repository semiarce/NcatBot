[tox]
envlist = py38,py39,py310,py311,py312
minversion = 4.0
requires = tox-uv
[testenv]
# 下面 3 行完全沿用旧习惯，但后台由 uv 装包
deps =
    pytest
    pytest-cov
commands = pytest {posargs:tests}

# 可选：让 uv 自动下载解释器（CI 很方便）
uv_python_preference = managed